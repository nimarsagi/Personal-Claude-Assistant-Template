{
  "_comment": "Phase 2 SessionEnd hook. Merge the 'hooks' key below into ~/.claude/settings.json via the hook-install section of INSTALL.md, replacing <assistant-path> with this folder's absolute path. This file itself is inert — nothing reads it at runtime.",
  "hooks": {
    "SessionEnd": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "<assistant-path>/install/hooks/session-end.sh",
            "timeout": 180
          }
        ]
      }
    ]
  }
}
