# Consolidation log

<!-- The memory-gardener appends ONE dated entry per run:
     YYYY-MM-DD — <short summary of what changed, or "no changes">
     The date must start the entry's first line; wrapping the rest
     onto further lines is fine. Boot checks the age of the newest
     line that starts with a date (overdue after 7 days).
     Append-only; never rewrite or delete past entries. -->
